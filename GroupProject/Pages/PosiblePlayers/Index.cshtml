@page
@model GroupProject.Pages.PosiblePlayers.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PosiblePlayer[0].PosiblePlayerName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PosiblePlayer[0].PlayerComments)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.PosiblePlayer) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PosiblePlayerName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PlayerComments)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.PosiblePlayerId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.PosiblePlayerId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.PosiblePlayerId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
